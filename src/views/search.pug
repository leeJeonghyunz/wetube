extends base.pug
include mixins/video

block content
    form(method="GET")
        input(placeholder="Search by title", type="text", name="keyword")
        input(type="submit", value="Search now")
    div
        each video in videos 
            +video(video)
            //- videos에 있는 파일들을 mixin으로 넘겨줌